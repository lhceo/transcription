# 実装ロードマップ（ROADMAP.md）

このファイルは「いま何が完了していて、次に何をやるか」を示す進捗管理ドキュメントです。

最終更新: 2026-05-12（v1.1 音声同期再生計画を追記）

---

## 現在のステータス

**フェーズ: ステージ B（技術仕様詳細化）完了 / ステージ C（実装）着手前**

- ✅ Mac アプリ版 v0.1 完成（社内未配布）
- ✅ 配布前検証で問題発覚（メモリ・MPS・速度）
- ✅ 設計判断完了（A〜J の全カテゴリ）
- ✅ ドキュメント基盤構築完了（10ファイル）
- ✅ Git タグ・ブランチ整備完了
- ✅ 技術スタック確定（FastAPI / HTMX+Alpine / SQLite / Railway）
- ✅ アーキテクチャ・データモデル・API・セキュリティ仕様確定
- ⏳ **次**: ステージ C（フェーズ 1: Web スケルトン実装）

---

## マイルストーン

### フェーズ 0: 凍結とドキュメント整備（完了）

**目的**: 過去の状態を保全し、知識を文書化する

- [x] `CLAUDE.md` 作成（Claude 用の道しるべ）
- [x] `docs/DESIGN.md` 作成（全設計決定）
- [x] `docs/DECISIONS.md` 作成（判断の経緯）
- [x] `docs/ROADMAP.md` 作成（本ファイル）
- [x] `docs/CHANGELOG.md` 作成・構造修正
- [x] `docs/RISKS.md` 作成（リスク・レジスター）
- [x] 未コミットの `transcribe_core.py` 変更を破棄（MPS 復帰実験）
- [x] Git タグ `v0.1-mac-app-snapshot` を作成（Mac アプリ最終状態の凍結）
- [x] ブランチ `web-app-rewrite` を作成

### フェーズ 1: Web アプリの骨格

**目的**: 「ブラウザで開ける」状態を作る。最小機能で動く土台

- [ ] 技術スタック選定の最終確定（FastAPI vs Flask など）
- [ ] プロジェクト構造の作成（`backend/`, `frontend/` など）
- [ ] 開発環境のセットアップ手順を `README.md` に記載
- [ ] 「Hello World」レベルの Web アプリが起動できる
- [ ] **タグ**: `v0.2-web-skeleton`

### フェーズ 2: 認証

**目的**: Google SSO で社内ユーザーだけがアクセスできる状態

- [ ] Google OAuth 設定（Google Cloud Console）
- [ ] バックエンドのログイン・ログアウト機能
- [ ] 社内ドメイン制限の実装
- [ ] セッション管理
- [ ] ログインしていない時のリダイレクト
- [ ] **タグ**: `v0.3-auth`

### フェーズ 3: ファイルアップロード

**目的**: ブラウザから音声ファイルを送れる

- [ ] フロントエンドのドラッグ&ドロップ UI
- [ ] バックエンドのアップロード受信
- [ ] 進捗表示（バイト数ベース）
- [ ] 2GB 上限のバリデーション
- [ ] **タグ**: `v0.4-upload`

### フェーズ 4: AssemblyAI 連携

**目的**: 文字起こしができる（話者分離含む）

- [ ] AssemblyAI API クライアント実装
- [ ] 非同期ジョブの管理（投入・ステータス確認・結果取得）
- [ ] ジョブ ID のサーバー側永続化
- [ ] 完了後の音声削除処理
- [ ] **タグ**: `v0.5-transcription`

### フェーズ 5: 結果表示と編集

**目的**: 文字起こし結果を見て編集できる

- [ ] カード形式の結果表示
- [ ] テキスト編集機能（ダブルクリックで編集モード）
- [ ] 話者編集 UI（F-1 の Notta 風）
- [ ] エクスポート機能（TXT / SRT / JSON）
- [ ] **タグ**: `v0.6-results`

### フェーズ 6: 履歴・一覧

**目的**: 過去の文字起こしを一覧・再アクセスできる

- [ ] 履歴一覧 UI（左ペイン）
- [ ] 文字起こしの保存と取り出し
- [ ] 検索・フィルタ
- [ ] **タグ**: `v0.7-history`

### フェーズ 7: コスト管理と運用

**目的**: 安心して長期運用できる

- [ ] コスト表示（処理後 + 月次累計）
- [ ] 長尺音声の警告（3時間超）
- [ ] エラーメッセージの整備
- [ ] ログ・監視
- [ ] **タグ**: `v0.8-operations`

### フェーズ 8: デプロイ

**目的**: 社内で実際に使える状態にする

- [ ] ホスティングサービスの選定（Railway / Render など）
- [ ] 本番環境のセットアップ
- [ ] AssemblyAI 支出上限の設定（C-1）
- [ ] 社員へのアクセス案内
- [ ] **タグ**: `v1.0-production`

### フェーズ 9 (v1.1): 音声同期再生（Notta 互換の聞きながら編集）

**目的**: ユーザーが音声を注意深く聞きながら、対応するセグメントを目で追い、編集・補足できる体験を提供する（Notta が標準で持つ機能）。

**ユーザーストーリー**: 「会議録音の文字起こし結果を見ながら、その場面の音声を聞き直し、誤認識の修正・補足・話者の特定をしたい。再生中は現在の発言箇所が自動でハイライト・スクロールされ、カードをクリックすればその時刻にジャンプする。」

#### 設計方針（**サーバー側に音声を保持しない**）

A-4「処理後音声即削除」ポリシーは維持する。代わりに**クライアント（ブラウザ）に音声ファイルを渡してローカル再生**する：

- 詳細画面に「音声ファイルを選択」ドロップゾーンを追加
- 選択された mp3/mp4 は `URL.createObjectURL()` で Blob URL 化し `<audio>` 要素で再生
- ファイルは**一切サーバーへ送られない**（機密性は現状維持）
- DB に既にある `segment.start_seconds` / `end_seconds` を用いて、`audio.currentTime` の `timeupdate` イベントで現在セグメントをハイライト・自動スクロール
- セグメントカードクリック → `audio.currentTime = segment.start_seconds` でシーク

#### 利点
- サーバー側の保管が不要（追加ストレージコスト ¥0）
- 機密性・セキュリティ方針の変更なし
- DB スキーマ変更なし（`start_seconds` / `end_seconds` は既存）
- フロント側だけの追加実装で完結

#### 利用上の前提
- ユーザーは元の音声ファイルを自分の Mac／OneDrive 等に保持しておく必要がある
- 過去案件を再聞きしたい場合、元音声を捨てていると再生不可（ただし**文字起こしテキストは引き続き閲覧・編集・エクスポート可能**）

#### タスク
- [ ] 詳細画面に音声ファイル選択 UI（ドロップゾーン + 「ファイルを開く」ボタン）
- [ ] HTML5 `<audio>` プレイヤー（再生・停止・シーク・速度変更）
- [ ] `timeupdate` イベントで現在再生時刻に該当するセグメントを判定しハイライト
- [ ] 再生中はアクティブカードを自動スクロールしてビューポート内に保つ（ユーザーが手動スクロールしている時は中断する配慮）
- [ ] セグメントクリックで該当時刻にシーク（編集モード中はクリック判定の取り合いに注意）
- [ ] 編集後の `start_seconds` ズレ補正（分割で生成された新セグメントは元の親の `start_seconds` から相対位置で算出される設計を確認）
- [ ] 速度変更・スキップ（±5秒）のキーボードショートカット（任意）
- [ ] **タグ**: `v1.1-audio-sync`

#### 受け入れ条件
- 音声ファイルを選択 → 再生 → 該当セグメントがハイライト・自動スクロールされる
- セグメントをクリック → その時刻に音声がジャンプする
- 音声ファイルを選択しない場合でも、現在の編集機能はそのまま動く（後方互換）

---

## v1.0.1 以降の改善候補（v1.0 リリース前レビューで指摘・先送り）

2026-05-12 の v1.0 リリース前レビューで指摘されたが、社内 5〜8 人用途・ベータ的位置づけということで一旦先送りとした項目。実利用フィードバックを見ながら優先度を判断する。

### セキュリティ / 堅牢性

- [ ] **CSRF トークンの導入**（POST/PATCH/DELETE エンドポイント）
  - 現状: SameSite=Lax + Google OAuth ドメイン制限で実害リスクは小さい
  - 必要性: SECURITY.md §5 で「重要操作のみ」と計画済み。特に `DELETE /api/transcripts/{id}` は本文不要なので、悪意ある外部サイトからの `<form method=POST>` 経由攻撃の可能性をゼロにはできない
- [ ] **`_record_speaker_history` の UniqueConstraint 違反ハンドリング**
  - 現状: select → insert 構造で、並行実行時に IntegrityError 発生の可能性
  - 影響: 単独ユーザーが 1 件編集中なら確率ほぼゼロ。複数タブ同時編集で潜在
- [ ] **`split_segment` の `order_index` 制約強化**
  - 現状: `(transcript_id, order_index)` の UNIQUE 制約なし
  - 影響: SQLite 単一ライターなので低リスクだが、将来 Postgres 化や複数ワーカー化したら必須

### UX / 表示

- [ ] **9 人以上の話者で色が重複する問題**
  - 現状: 色は 8 色ループなので、9 人目以降は既存話者と同色になる
  - 影響: 5〜8 人想定のため通常運用で問題なし。長尺会議で発生時に「色は手がかりの 1 つ」と認識すれば許容範囲
  - 改修案: 9 人目以降は枠線パターン違いで識別、または色数を 12 まで拡張
- [ ] **`transcript.status` の中間段階を可視化**
  - 現状: `pending` → `processing` → `completed/failed` の遷移しか持たない
  - 改修案: `uploading_to_assemblyai` / `polling` などの中間状態を導入し UI に表示

### コード品質 / メンテナンス性

- [ ] **`routes.py:465` の `if False else` 残骸を削除**（無害だが紛らわしい）
- [ ] **`transcript_detail.html:418` の `setAttribute('@click', ...)` を削除**（Alpine に無視される死コード）
- [ ] **`assemblyai_client.delete_transcript` のタイムアウト短縮**
  - 現状: AssemblyAI 障害時にジョブ完了処理が最大 60 秒ブロック
- [ ] **`closeEditor()` が `input/segmentId` を初期化していない**
  - 影響: ポップアップ再オープン時、1 フレームだけ前回入力が残る（視認上問題なし）
- [ ] **`_user_speaker_history_names` の局所 import を module-level に**

### 旧 API のクリーンアップ

- [ ] **`PATCH /api/transcripts/{id}/speakers/{speaker_label}` を削除**
  - 現状: Notta 方式リネーム導入後、フロントから呼び出されていないが、サーバー側コードは残置
  - 一定期間モニタリング後（v1.1 など）に削除

---

## 次のアクション

### v1.0 リリース — 完了（2026-05-12）

- [x] ステージ B（技術仕様の詳細化）: B-1 〜 B-5 すべて完了
- [x] フェーズ 1 〜 8（Web スケルトン → 本番デプロイ）すべて完了
- [x] タグ `v1.0-production` を切って本番稼働開始

### 次にやること

1. **v1.0.1 クリーンアップ**（任意、v1.1 着手前推奨）
   - 上記「v1.0.1 以降の改善候補」から、v1.1 で `transcript_detail.html` を触る前に片付けたい死コード掃除を中心に
   - 旧 API `PATCH /api/transcripts/{id}/speakers/{speaker_label}` 削除
2. **v1.1 音声同期再生**（フェーズ 9）
   - 上記「フェーズ 9」セクションのタスクに沿って実装
   - タグ: `v1.1-audio-sync`

---

## 過去のオープン決定事項（すべて確定済み）

v1.0 リリース時点で以下は全て確定：

- バックエンドフレームワーク: **FastAPI** で確定
- フロントエンドフレームワーク: **Jinja2 + Alpine.js + HTMX**（素の HTML/JS 寄り）で確定
- データベース: **SQLite + Alembic** で確定（将来 Postgres 化の余地は残す）
- ホスティング: **Railway** で確定
- 文字起こし結果の保持期間: **永続**（社内 5〜8 人運用のため）で確定

---

## 未着手だが意識しておきたい論点

- 既存の Mac アプリで作った `.transcription.json` の Web 版への取り込み機能（D-1）
- 複数人共同編集（将来要望に応じて）
- スマホ対応（将来要望に応じて）
- バックアップ戦略

---

## 進捗の更新ルール

- フェーズ完了時に該当タグを設定して、ここのチェックボックスを更新
- 新たな決定事項が出たら `DECISIONS.md` にも追記
- 大きな仕様変更があったら `DESIGN.md` を更新
